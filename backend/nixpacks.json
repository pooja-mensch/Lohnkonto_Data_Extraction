{
  "phases": {
    "setup": {
      "nixPkgs": ["python311", "openjdk17"]
    },
    "install": {
      "cmds": [" pip3 install --no-cache-dir -r requirements.txt"]
    }
  },
  "start": {
    "cmd": "/usr/bin/env uvicorn api:app --host 0.0.0.0 --port $PORT"
  }
}
